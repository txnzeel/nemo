name: nemo
version: '1.0'
config-version: 2
profile: nemo
model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
models:
  nemo:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: analytics
